import styled from "styled-components";

export const ProfileStyle = styled.div`
  .profile {
    position: relative;

    .modal {
      transition: 0.5s;
      position: absolute;
      top: 0;
    }
  }
`